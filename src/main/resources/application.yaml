server:
  port: 8080

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5678/commercehub_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:123}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: ${JPA_SHOW_SQL:false}
    properties:
      hibernate:
        format_sql: ${JPA_FORMAT_SQL:false}

  data:
    web:
      pageable:
        default-page-size: 20
        max-page-size: 100

google:
  client-id: "1060424015252-neu3kv70tuss4g3md3g74njho7mihpja.apps.googleusercontent.com"

jwt:
  secret: ${JWT_SECRET:V29ya2luZ0hhcmRPbkNvbW1lcmNlSHViUHJvamVjdDIwMjZTZWN1cmVLZXlWMzg5NDU=}
  expiration: 900000 # Access Token 15 phút
  refreshExpiration: 604800000 # Thời gian sống của Refresh Token7 ngày = 604800000 ms

auth:
  refresh-cookie:
    name: ${AUTH_REFRESH_COOKIE_NAME:commercehub_refresh}
    # Localhost chạy HTTP phải để false. Production bắt buộc AUTH_COOKIE_SECURE=true.
    secure: ${AUTH_COOKIE_SECURE:false}
    same-site: ${AUTH_COOKIE_SAME_SITE:Strict}
    max-age-seconds: 604800


#Local/development: SPRINGDOC_ENABLED=true.
#CI: giữ true vì có contract test kiểm tra /v3/api-docs.
#Production: nên đặt SPRINGDOC_ENABLED=false nếu không muốn công khai cấu trúc API.
springdoc:
  api-docs:
    enabled: ${SPRINGDOC_ENABLED:true}
  swagger-ui:
    enabled: ${SPRINGDOC_ENABLED:true}
    operations-sorter: method
    tags-sorter: alpha
    display-request-duration: true

sepay:
  environment: ${SEPAY_ENVIRONMENT:test}
  webhook-secret: ${SEPAY_WEBHOOK_SECRET:replace-with-sepay-webhook-secret}
  bank-code: ${SEPAY_BANK_CODE:replace-with-bank-code}
  bank-account-number: ${SEPAY_BANK_ACCOUNT_NUMBER:replace-with-bank-account-number}
  account-name: ${SEPAY_ACCOUNT_NAME:replace-with-account-name}
  qr-base-url: ${SEPAY_QR_BASE_URL:https://vietqr.app/img}
  deposit-ttl-minutes: 15
  deposit-expiry-scan-ms: 60000
  create-cooldown-seconds: 10
  max-creates-per-fifteen-minutes: 5
  max-creates-per-day: 20

commercehub:
  jobs:
    batch-size: ${JOB_BATCH_SIZE:200}
  dispute:
    seller-response-hours: ${DISPUTE_SELLER_RESPONSE_HOURS:24}
    warranty-processing-hours: ${DISPUTE_WARRANTY_PROCESSING_HOURS:24}
    buyer-confirmation-hours: ${DISPUTE_BUYER_CONFIRMATION_HOURS:24}
    admin-review-hours: ${DISPUTE_ADMIN_REVIEW_HOURS:72}

#  server:
#    port: ${SERVER_PORT:8080}
#
#  spring:
#    datasource:    
#      url: ${DB_URL}
#      username: ${DB_USERNAME}
#      password: ${DB_PASSWORD}
#
#    jpa:
#      hibernate:
#        ddl-auto: validate
#      show-sql: false
#      properties:
#        hibernate:
#          format_sql: false
#
#  google:
#    client-id: ${GOOGLE_CLIENT_ID}
#
#  jwt:
#    secret: ${JWT_SECRET}
#    expiration: ${JWT_ACCESS_EXPIRATION_MS:900000}
#    refreshExpiration: ${JWT_REFRESH_EXPIRATION_MS:604800000}
#
#  auth:
#    refresh-cookie:
#      name: ${AUTH_REFRESH_COOKIE_NAME:commercehub_refresh}
#      secure: ${AUTH_COOKIE_SECURE:true}
#      same-site: ${AUTH_COOKIE_SAME_SITE:Strict}
#      max-age-seconds: ${AUTH_REFRESH_COOKIE_MAX_AGE:604800}
#
#  sepay:
#    environment: ${SEPAY_ENVIRONMENT:production}
#    webhook-secret: ${SEPAY_WEBHOOK_SECRET}
#    bank-code: ${SEPAY_BANK_CODE}
#    bank-account-number: ${SEPAY_BANK_ACCOUNT_NUMBER}
#    account-name: ${SEPAY_ACCOUNT_NAME}
#    qr-base-url: ${SEPAY_QR_BASE_URL:https://vietqr.app/img}
#    deposit-ttl-minutes: 15
#    deposit-expiry-scan-ms: 60000
#    create-cooldown-seconds: 10
#    max-creates-per-fifteen-minutes: 5
#    max-creates-per-day: 20
#
#
